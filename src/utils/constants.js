const surveyAttrs = {
  costPerResponse: 5,
  costPerQuestion: 100,
};

const adAttrs = {
  costPerView: 3,
}

const reelAttrs = {
  costPerView: 0.5,
}

const ytVideoAttrs = {
  costPerView: 3,
}

const dayRangeMultipliers = {
  30: 0.25,
  45: 0.2,
  60: 0.15,
  90: 0.1,
};

export { surveyAttrs, adAttrs, reelAttrs, ytVideoAttrs, dayRangeMultipliers };